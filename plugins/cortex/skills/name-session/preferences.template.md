# name-session preferences

## recurring_commands
<!-- Comma-separated slash commands whose sessions recur on a date cadence (e.g. /daily-review, /weekly-triage). Sessions driven by these get today's date appended to the name. Leave empty to rely on judgment only. -->
recurring_commands:

## copy_to_clipboard
<!-- true = copy the /rename command to the clipboard when a clipboard tool is available; false = just print it. Default: true -->
copy_to_clipboard: true
