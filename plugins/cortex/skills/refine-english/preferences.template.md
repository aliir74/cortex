# refine-english preferences

Copy this file to `${CLAUDE_PLUGIN_DATA}/preferences/refine-english.md` and fill in your values. The skill reads that copy, not this template, so your values survive plugin updates.

Any field left empty means "use the default documented in SKILL.md."

## native_language
<!--
Your first language (e.g. Spanish, Persian, Mandarin). Used to spot interference
patterns and to explain in that language when you write to Claude in it.
Default: (empty, no language-specific tailoring)
-->
native_language:

## copy_to_clipboard
<!--
true = copy the refined version to the clipboard after each refinement.
Default: false
-->
copy_to_clipboard:
